PORT=3000
APP_URL=http://localhost:3000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-password
DB_NAME=harvesthub

JWT_SECRET=generate-a-long-random-string
JWT_EXPIRES_IN=30d

UPLOAD_DIR=./uploads

# console = log OTP to terminal (dev). Swap for msg91/twilio in prod.
SMS_PROVIDER=console
SMS_API_KEY=

# mock = fake gateway (dev). Swap for razorpay in prod.
PAYMENT_PROVIDER=mock
PAYMENT_KEY_ID=
PAYMENT_KEY_SECRET=
PAYMENT_WEBHOOK_SECRET=
