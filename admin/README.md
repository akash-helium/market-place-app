# HarvestHub Admin

Ops panel for KYC approval and shop management.

```bash
cd admin
npm install
npm run dev
```

Open http://localhost:5173

Default admin key (also in sidebar): `harvesthub-admin-dev-key`  
Must match backend `ADMIN_API_KEY` in `.env`.
